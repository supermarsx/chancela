# Docker Deployment Profiles

The Docker Compose deployment profiles (`single-node`, `validation-worker`, `postgres`), their hardening posture, Postgres backup/restore, and HA limits are documented on the docs site: **[Deployment](https://supermarsx.github.io/chancela/deployment/)** (source: [`docs/deployment.md`](../docs/deployment.md)).

{
  "id": "assoc-termo-encerramento/v3",
  "family": "Association",
  "stage": "TermoEncerramento",
  "channels": [],
  "signature_policy": "ManualAttested",
  "rule_pack_id": "assoc-cc/v1",
  "locale": "pt-PT",
  "blocks": [
    {
      "kind": "Heading",
      "level": 1,
      "template": "Termo de encerramento do livro de atas"
    },
    {
      "kind": "Paragraph",
      "template": "Aos {{ closing_date | long_date }}, procede-se ao encerramento do presente livro de atas da associação {{ entity.name }}{% if entity.nipc %}, pessoa coletiva n.º {{ entity.nipc }}{% endif %}{% if book.kind %}, respeitante ao órgão {{ book.kind }}{% endif %}{% if reason == 'BookFull' %}, por se encontrar o livro esgotado{% elif reason == 'EntityDissolved' %}, por extinção da associação{% elif reason == 'MigrationToSuccessor' %}, por migração para livro sucessor{% elif reason.Other is defined %}, pelo motivo seguinte: {{ reason.Other.note }}{% elif reason %}, pelo motivo seguinte: {{ reason }}{% endif %}."
    },
    {
      "kind": "Paragraph",
      "template": "{% if ata_count == 1 %}O livro contém {{ ata_count }} ({{ ata_count | in_words(gender='f') }}) ata, lavrada e numerada sequencialmente, cuja integridade se encontra assegurada pela cadeia de registo desta escrituração digital.{% elif ata_count is defined %}O livro contém {{ ata_count }} ({{ ata_count | in_words(gender='f') }}) atas, lavradas e numeradas sequencialmente, cuja integridade se encontra assegurada pela cadeia de registo desta escrituração digital.{% endif %}"
    },
    {
      "kind": "Paragraph",
      "items": "body",
      "template": "{% if heading %}{{ heading }} — {% endif %}{{ text }}"
    },
    {
      "kind": "Rule"
    },
    {
      "kind": "SignatureBlock",
      "source": "required_signatories",
      "role": "{{ role | role_label }}",
      "name": "{{ name }}"
    }
  ],
  "default_body": [
    {
      "text": "Nada mais havendo a lavrar no presente livro, dá-se o mesmo por encerrado. Lavra-se o presente termo de encerramento, que vai assinado por quem nele intervém."
    }
  ]
}

//! The `ScapTransport` trait (list_providers / fetch_attributes / sign_with_attrs) plus its
//! `HttpScapTransport` (blocking `reqwest`) and fixture-backed `MockScapTransport` implementations.
//! The mock is the default and the only transport exercised by tests.
//!
//! **Status:** skeleton (t67-e0). Filled by t67-e4.
